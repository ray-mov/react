import styled from "styled-components";

const StyledHeader = styled.header`
  background-color: #00d5ff;
  padding: 1.2 4.8rem;
  border-bottom: 1px solid gray;
`;

const Header = () => {
  return <StyledHeader>Hader</StyledHeader>;
};

export default Header;
